<local:ForegroundWindow
    x:Class="HuntAndPeck.Views.OverlayView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="clr-namespace:HuntAndPeck.Views"
    WindowStyle="None"
    ResizeMode="NoResize"
    ShowInTaskbar="False"
    AllowsTransparency="True"
    Topmost="True"
    FocusManager.FocusedElement="{Binding ElementName=MatchStringControl}"
    Loaded="OverlayView_OnLoaded"
    PreviewKeyDown="OverlayView_OnPreviewKeyDown">
    <local:ForegroundWindow.Resources>
        <Style x:Key="HintBorderStyle" TargetType="Border">
            <Setter Property="Padding" Value="7 3" />
            <Setter Property="CornerRadius" Value="4" />
        </Style>
    </local:ForegroundWindow.Resources>
    <local:ForegroundWindow.Background>
        <SolidColorBrush Color="Transparent" />
    </local:ForegroundWindow.Background>
    <Grid x:Name="layoutGrid">
        <TextBox x:Name="MatchStringControl" Text="{Binding MatchString, UpdateSourceTrigger=PropertyChanged, Mode=OneWayToSource}" Width="50" VerticalAlignment="Bottom" Background="Transparent" Foreground="White" />
        <ItemsControl ItemsSource="{Binding Hints}">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <Canvas IsItemsHost="True" />
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>

            <ItemsControl.ItemContainerStyle>
                <Style>
                    <Setter Property="Canvas.Top" Value="{Binding Hint.BoundingRectangle.Top}" />
                    <Setter Property="Canvas.Left" Value="{Binding Hint.BoundingRectangle.Left}" />
                </Style>
            </ItemsControl.ItemContainerStyle>

            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Grid>
                        <!--<Rectangle Width="{Binding Hint.BoundingRectangle.Width}" Height="{Binding Hint.BoundingRectangle.Height}" Stroke="Red" StrokeDashArray="1 2">
                            <Rectangle.Fill>
                                <SolidColorBrush Color="Yellow" Opacity="0.05" />
                            </Rectangle.Fill>
                        </Rectangle>-->
                        <Viewbox StretchDirection="DownOnly" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="1 1 0 0" Width="{Binding Hint.BoundingRectangle.Width}" Height="{Binding Hint.BoundingRectangle.Height}">
                            <Border Padding="4 1" CornerRadius="3" BorderThickness="1">
                                <Border.Style>
                                    <Style TargetType="Border">
                                        <Setter Property="Background">
                                            <Setter.Value>
                                                <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                                                    <GradientStop Color="#FFFEF5" Offset="0"/>
                                                    <GradientStop Color="#FFE44D" Offset="0.3"/>
                                                    <GradientStop Color="#F5D033" Offset="0.7"/>
                                                    <GradientStop Color="#E8C028" Offset="1"/>
                                                </LinearGradientBrush>
                                            </Setter.Value>
                                        </Setter>
                                        <Setter Property="BorderBrush">
                                            <Setter.Value>
                                                <SolidColorBrush Color="#C9A825" />
                                            </Setter.Value>
                                        </Setter>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding Active}" Value="false">
                                                <Setter Property="Background">
                                                    <Setter.Value>
                                                        <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                                                            <GradientStop Color="#F5F3E8" Offset="0"/>
                                                            <GradientStop Color="#EDDA3F" Offset="0.3"/>
                                                            <GradientStop Color="#E0C230" Offset="0.7"/>
                                                            <GradientStop Color="#D4B022" Offset="1"/>
                                                        </LinearGradientBrush>
                                                    </Setter.Value>
                                                </Setter>
                                                <Setter Property="BorderBrush">
                                                    <Setter.Value>
                                                        <SolidColorBrush Color="#B89920" />
                                                    </Setter.Value>
                                                </Setter>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Border.Style>
                                <TextBlock Text="{Binding Label}" FontFamily="{Binding FontFamilyReadValue}" FontWeight="Bold" FontSize="{Binding FontSizeReadValue}" RenderOptions.ClearTypeHint="Enabled" TextOptions.TextRenderingMode="ClearType">
                                    <TextBlock.Foreground>
                                        <SolidColorBrush Color="{Binding TextColorReadValue}" />
                                    </TextBlock.Foreground>
                                    <TextBlock.Effect>
                                        <DropShadowEffect Color="#50000000" BlurRadius="0.8" ShadowDepth="0.3" Opacity="0.5" Direction="270" RenderingBias="Quality"/>
                                    </TextBlock.Effect>
                                </TextBlock>
                            </Border>
                        </Viewbox>
                    </Grid>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
        </Grid>
</local:ForegroundWindow>

